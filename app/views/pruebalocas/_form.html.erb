<%= form_for(@pruebaloca) do |f| %>
  <% if @pruebaloca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pruebaloca.errors.count, "error") %> prohibited this pruebaloca from being saved:</h2>

      <ul>
      <% @pruebaloca.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :pelos %><br />
    <%= f.number_field :pelos %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
