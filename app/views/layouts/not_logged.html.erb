<!DOCTYPE html>
<html>
<head>
  <title>Blogger</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%=javascript_include_tag :defaults %>
  <%= csrf_meta_tags %>  
</head>
<body>
  
  <div class = "content">
    <div class = "header">
      <% if (user_signed_in?) %>
        <div class ="link"><%= link_to 'Logout', users_logout_path %></td></div>
      <% else %>
        <div class ="link"><%= link_to 'Login', users_show_login_path %></td></div>
      <%end%>
      <div class ="link"><%= link_to 'Search', search_index_path %></td> </div>
    </div>
    <div class = "info">
    <%= yield %>
    </div>
    <div class = "sidebar">
      <% if (user_signed_in?) %>
        <div class="link"><%= link_to 'Edit my account', edit_user_path(current_user) %></div>
        <%= link_to image_tag(current_user.image), user_path(current_user)%>
        <h1 class = "inside_title">My tags:</h1>
        <% tags = Tag.my_tags(current_user.id).order() %>
        <% tags.uniq.each do |t| %>
          <%= link_to t.label, tag_path(t.id) %>
        <% end%>
      <%end%>

    </div>
    <div class = "footer">
      footer!!!
    </div>
  </div>
</body>
</html>
