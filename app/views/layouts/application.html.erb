<!DOCTYPE html>
<html>
<head>
  <title>Blogger</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%=javascript_include_tag :defaults %>
  <%= csrf_meta_tags %>  
</head>
<body>  
  <div class = "content">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class = "header">
      <% if (user_signed_in?) %>
        <div class ="link"><%= link_to 'Logout', destroy_user_session_path, :method=>:delete %></td></div>
      <%end%>
      <div class ="link"><%= link_to 'Search', search_index_path %></td> </div>
    </div>
    <div class = "info">
    <%= yield %>
    </div>
    <div class = "sidebar">
      <% if (user_signed_in?) %>
        <div class="link"><%= link_to 'Edit my account', edit_user_registration_path %></div>
        <%= link_to image_tag(current_user.image), user_path(current_user)%>
        <br><br><br><br>
     </td>
      <br>
      <h1 class = "inside_title">Followed:</h1>

      <% current_user.get_followed.each do |f| %>
        <div class ="link"><%= link_to f[:followable_name], user_show_public_path(f[:followable_id]) %></div><br><br>
      <% end%><br>
      <h1 class = "inside_title">Suggested:</h1>

      <% current_user.get_not_followed.each do |f| %>
        <div class ="link"><%= link_to f[:not_followable_name], user_show_public_path(f[:not_followable_id]) %></div>
      <% end%>
        <h1 class = "inside_title">My tags:</h1>
        <% tags = Tag.my_tags(current_user.id).order() %>
        <% tags.uniq.each do |t| %>
          <%= link_to t.label, tag_path(t.id) %>
        <% end%>
      <%end%>
    </div>
    <div class = "footer">
      footer!!!
    </div>
  </div>
</body>
</html>
